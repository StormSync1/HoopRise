# Security policy

HoopRise is a local-only application. It does not provide server accounts or cloud authentication.

Do not report private profile data, PINs, recovery answers, or screenshots containing them in public issues. For a suspected security problem, open a private GitHub security advisory when available or contact the repository owner directly.

The local recovery question is weaker than real authentication. Never use an answer that another person could easily guess.
