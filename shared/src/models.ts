export type Position = 'Point Guard'|'Shooting Guard'|'Small Forward'|'Power Forward'|'Center';
export type DrillCategory = 'Shooting'|'Ball handling'|'Finishing'|'Passing'|'Defense'|'Rebounding'|'Footwork'|'Speed'|'Agility'|'Conditioning'|'Athleticism'|'Free throws'|'Three-point shooting'|'Mid-range shooting'|'Warmups'|'Stretching';
export type GameRecord = { id:string; opponent:string; date:string; scoreFor:number; scoreAgainst:number; result:'W'|'L'; minutes:number; points:number; rebounds:number; assists:number; steals:number; blocks:number; turnovers:number; fouls:number; fgMade:number; fgAttempted:number; threeMade:number; threeAttempted:number; ftMade:number; ftAttempted:number; notes:string; season:string; };
export type Drill = { id:string; name:string; category:DrillCategory; difficulty:'Easy'|'Medium'|'Hard'; recommendedMinutes:number; equipment:string; goal:string; steps:string[]; coachingCue?:string; tags:string[]; };
export type TrainingSession = { id:string; name:string; category:DrillCategory; durationMinutes:number|null; elapsedSeconds:number; drillIds:string[]; goal:string; notes:string; makes:number; date:string; xp:number; completed:boolean; };
export type Profile = { firstName:string; lastName:string; position:Position; jerseyNumber:string; team:string; favoriteTeam:string; height:string; age?:number; season:string; imageData?:string; recoveryQuestion:string; recoveryHash:string; recoverySalt:string; pinHash?:string; pinSalt?:string; };
export type Goal = { id:string; type:'minutes'|'makes'|'workouts'|'days'|'threePct'|'points'; title:string; target:number; current:number; period:'week'|'season'|'career'; completed:boolean; };
export type PersonalRecords = Record<string,{value:number; date:string; label:string}>;
export type AppState = { profile?:Profile; games:GameRecord[]; sessions:TrainingSession[]; customDrills:Drill[]; goals:Goal[]; records:PersonalRecords; xp:number; level:number; settings:{voice:boolean; locked:boolean}; };
